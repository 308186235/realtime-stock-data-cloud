{
  "name": "aigupiao-subdomain-router",
  "version": "1.0.0",
  "description": "Cloudflare Worker for aigupiao.me subdomain routing",
  "main": "worker.js",
  "scripts": {
    "deploy": "wrangler deploy"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
