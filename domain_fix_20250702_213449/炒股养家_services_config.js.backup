/**
 * 服务配置文件 - 统一配置
 */

// API基础URL,根据环境设置
const baseUrl = 'https://ai-stock-trading-backend.308186235.workers.dev';

// 超时设置(毫秒)
const timeout = 30000;

// 重试次数
const retryCount = 3;

// 导出配置
export {
  baseUrl,
  timeout,
  retryCount
};