# 混合交易系统用户操作手册

## 📋 系统概述

本系统是一个完整的混合交易解决方案,结合了本地Windows交易软件和云端Ubuntu服务器,通过Cloudflare Zero Trust VPN实现安全连接。

### 🏗️ 系统架构
```
本地Windows系统 ←→ Cloudflare Zero Trust ←→ 阿里云Ubuntu服务器
     ↓                    ↓                      ↓
  交易软件执行          安全隧道传输           数据分析处理
  数据导出              域名路由              云端存储
  API服务              负载均衡              监控告警
```

### 🔧 核心组件
- **本地交易API** (端口5000): 处理交易操作和数据导出
- **本地主API** (端口8000): 主要API服务和WebSocket
- **本地交易系统** (端口8888): 完整交易系统功能
- **本地Agent** (端口9999): 智能分析代理
- **Cloudflare隧道**: 安全连接云端服务
- **云端服务**: Ubuntu服务器上的分析和存储服务

## 🚀 快速启动指南

### 1. 系统启动顺序

#### 第一步:启动本地服务
```bash
# 1. 启动本地交易API
python local_trading_api.py

# 2. 启动主API服务
python backend/start_server.py

# 3. 启动交易系统
python complete_cloud_local_trading_system.py

# 4. 启动Agent后端
python local_agent_backend.py
```

#### 第二步:启动Cloudflare隧道
```bash
# 启动隧道
python -c "from zero_trust_manager import ZeroTrustManager; ZeroTrustManager().start_tunnel()"

# 或使用重启脚本
python restart_tunnel.py
```

#### 第三步:验证系统状态
```bash
# 运行系统验证
python test_end_to_end_integration.py
```

### 2. 一键启动脚本

创建 `start_all_services.bat` 文件:
```batch
@echo off
echo 启动混合交易系统...

start "交易API" python local_trading_api.py
timeout /t 3
start "主API" python backend/start_server.py  
timeout /t 3
start "交易系统" python complete_cloud_local_trading_system.py
timeout /t 3
start "Agent" python local_agent_backend.py
timeout /t 5
start "隧道" python restart_tunnel.py

echo 所有服务已启动,请等待初始化完成...
pause
```

## 📊 系统监控

### 实时监控
```bash
# 启动系统监控 (每60秒检查一次)
python system_monitor.py

# 单次状态检查
python test_monitor.py
```

### 监控指标
- **系统资源**: CPU,内存,磁盘使用率
- **服务状态**: 所有本地和云端服务健康状态
- **网络连接**: Cloudflare隧道连接质量
- **响应时间**: API服务响应性能
- **交易软件**: 交易程序运行状态

### 告警机制
- 🔴 **严重告警**: 服务宕机,隧道断开
- 🟡 **警告**: 响应时间过长,资源使用率高
- 🟢 **正常**: 所有服务运行正常

## 🔧 日常操作

### 交易操作

#### 1. 余额查询
```bash
curl http://localhost:5000/balance
```

#### 2. 执行交易
```bash
# 买入操作
curl -X POST http://localhost:5000/trade \
  -H "Content-Type: application/json" \
  -d '{"action":"buy","code":"000001","quantity":100,"price":10.0}'

# 卖出操作  
curl -X POST http://localhost:5000/trade \
  -H "Content-Type: application/json" \
  -d '{"action":"sell","code":"000001","quantity":100,"price":10.0}'
```

#### 3. 数据导出
```bash
# 导出持仓数据
curl -X POST http://localhost:5000/export \
  -H "Content-Type: application/json" \
  -d '{"type":"holdings"}'

# 导出交易记录
curl -X POST http://localhost:5000/export \
  -H "Content-Type: application/json" \
  -d '{"type":"transactions"}'
```

### 系统维护

#### 1. 服务重启
```bash
# 重启特定服务 (以交易API为例)
# 1. 找到进程ID
netstat -ano | findstr :5000
# 2. 终止进程
taskkill /PID <进程ID> /F
# 3. 重新启动
python local_trading_api.py
```

#### 2. 隧道维护
```bash
# 检查隧道状态
python -c "from zero_trust_manager import ZeroTrustManager; print(ZeroTrustManager().get_tunnel_status())"

# 重启隧道
python restart_tunnel.py
```

#### 3. 日志查看
```bash
# 查看系统监控日志
type system_monitor.log

# 查看优化日志
type optimization.log
```

## 🛠️ 故障排除

### 常见问题及解决方案

#### 1. 服务无法启动
**症状**: 端口被占用或服务启动失败
**解决方案**:
```bash
# 检查端口占用
netstat -ano | findstr :5000
# 终止占用进程
taskkill /PID <进程ID> /F
# 重新启动服务
```

#### 2. 隧道连接失败
**症状**: 无法访问云端服务
**解决方案**:
```bash
# 1. 检查网络连接
ping api.aigupiao.me
# 2. 重启隧道
python restart_tunnel.py
# 3. 检查配置文件
type config.yml
```

#### 3. 交易软件无响应
**症状**: 交易操作失败或超时
**解决方案**:
```bash
# 1. 按F5刷新交易软件
# 2. 检查交易软件进程
tasklist | findstr "交易"
# 3. 重启交易相关服务
```

#### 4. API响应缓慢
**症状**: 接口调用超时
**解决方案**:
```bash
# 1. 检查系统资源
python -c "import psutil; print(f'CPU: {psutil.cpu_percent()}%, 内存: {psutil.virtual_memory().percent}%')"
# 2. 运行性能优化
python system_optimization.py
# 3. 重启相关服务
```

### 紧急恢复程序

#### 完全系统重启
```bash
# 1. 停止所有服务
taskkill /F /IM python.exe
taskkill /F /IM cloudflared.exe

# 2. 等待5秒
timeout /t 5

# 3. 重新启动所有服务
start_all_services.bat
```

## 📈 性能优化建议

### 1. 系统资源优化
- **CPU使用率** < 80%: 关闭不必要的程序
- **内存使用率** < 85%: 定期清理内存
- **磁盘使用率** < 90%: 清理临时文件

### 2. 网络优化
- 使用有线网络连接 (如可能)
- 优化移动热点设置
- 定期检查网络延迟

### 3. 服务优化
- 定期重启长时间运行的服务
- 监控API响应时间
- 优化数据库查询

## 🔒 安全注意事项

### 1. 访问控制
- 仅在本地网络访问API服务
- 定期更新Cloudflare隧道配置
- 监控异常访问日志

### 2. 数据安全
- 定期备份交易数据
- 加密敏感配置文件
- 使用强密码保护账户

### 3. 网络安全
- 保持Cloudflare隧道连接
- 监控网络流量异常
- 定期更新安全补丁

## 📞 技术支持

### 联系方式
- **系统监控**: 查看 `system_monitor.log`
- **性能报告**: 查看 `optimization_report.json`
- **集成测试**: 运行 `test_end_to_end_integration.py`

### 自助诊断
```bash
# 完整系统检查
python test_end_to_end_integration.py

# 性能和安全检查  
python system_optimization.py

# 实时监控
python system_monitor.py
```

### 日志文件位置
- `system_monitor.log`: 系统监控日志
- `optimization.log`: 优化操作日志
- `integration_test_report.json`: 集成测试报告
- `optimization_report.json`: 优化报告

---

## 📝 版本信息
- **系统版本**: 3.0.0
- **最后更新**: 2025-07-06
- **兼容性**: Windows 10/11, Python 3.13+
- **依赖服务**: Cloudflare Zero Trust, 阿里云Ubuntu

**注意**: 本手册基于当前系统配置编写,如有配置变更请及时更新手册内容。
